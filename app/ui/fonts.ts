import { Inter, Lusitana } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

const luisitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'] });

export { inter, luisitana };
